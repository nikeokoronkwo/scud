## Project

```scudfile
project(NAME, [lang: LANGUAGE, version: VERSION])
```

## Executable

```scudfile
executable(
    NAME,
    src: SOURCES[],
    include: INCLUDES[]
)
```

## Library

```scudfile
library(
    NAME,
    src: SOURCES[],
    include: INCLUDES[],
)
```